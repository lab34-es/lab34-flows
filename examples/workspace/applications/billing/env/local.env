# Environment file for the "billing" application, environment "local".
# The example flows mimic this API locally on port 4545 (see mimic.js),
# so no real billing service is needed.
BASE_URL=http://localhost:4545
