# Environment "local". Add another environment by copying this file as
# env/<name>.env: the UI lists every environment any application declares, and
# a flow runs against the selected one.
#
# BASE_URL is what httpClient prefixes every request with.
BASE_URL=https://httpbin.org
